%h2 Email ID: #{@user.email}
.row
  .col-md-4.col-md-offset-4.center
    = gravatar_for @user
    = link_to 'Edit Profile', edit_user_registration_path

%h3 Events Added:

- @user.events.each do |event|
  .row
    .col-md-10.col-md-offset-1.col-xs-12
      = link_to event.EventDate, event_path(event)
      
